# Never commit the real .env — it is gitignored.
OPENAI_API_KEY=sk-...

# openai (default) | youcam
CUTOUT_PROVIDER=openai
PORT=8787

# Bright Data Web Unlocker — only used for retailers that refuse plain server
# fetches (Amazon, Zara, H&M, Nike, SHEIN…). Without it those hosts fail with a
# clear error; everything else still works.
BRIGHTDATA_API_KEY=
BRIGHTDATA_ZONE=
# Optional two-letter exit country, e.g. us
BRIGHTDATA_COUNTRY=

# Google Sign-In. Set to the OAuth *web* client id (comma-separate the iOS and
# Android ones too if their tokens reach this server). While unset, the server
# runs in dev mode and accepts the client's `dev:` stand-in sign-in.
GOOGLE_CLIENT_ID=
